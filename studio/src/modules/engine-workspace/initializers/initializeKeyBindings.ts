import type { Bifrost } from '#bifrost/Bifrost';

export default function initializeKeyBindings(_bifrost: Bifrost): void {
  // Keybindings are now registered in initializeRunMenu.ts (G3).
  // F3 = Deploy Current Process
  // Shift+F3 = Deploy & Open Current Process
  // F5 = Quick Deploy & Start in Debugger
  // Shift+F5 = Configured Start in Debugger
}
