import type { Bifrost } from '#bifrost/Bifrost';

export default function initializeMenus(_bifrost: Bifrost): void {
  // No menus to register — auto-refresh is always immediate on WebSocket events.
}
